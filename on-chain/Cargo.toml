[package]
name = "on-chain"
version = "0.1.0"
edition = "2021"

[dependencies]
bitcoin = "0.26.2"
bitcoincore-rpc = "0.17.0"  # 最新的 Bitcoin Core RPC 库
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"